# Drop-in for ssh.service — make sshd unkillable by the OOM killer
# Install: sudo mkdir -p /etc/systemd/system/ssh.service.d/
#          sudo cp ssh-oom-protect.conf /etc/systemd/system/ssh.service.d/override.conf
#          sudo systemctl daemon-reload
[Service]
OOMScoreAdjust=-1000
